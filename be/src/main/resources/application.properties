# Database connection settings
spring.datasource.url=jdbc:postgresql://localhost:5432/news_db
spring.datasource.username=admin
spring.datasource.password=admin123
spring.datasource.driver-class-name=org.postgresql.Driver

# Disable automatic schema generation
spring.jpa.hibernate.ddl-auto=update

# Bật chế độ tự động tạo schema từ file schema.sql
spring.sql.init.mode=NEVER

# JWT Configuration
app.jwt.secret=verySecretKeyThatShouldBeAtLeast32CharactersLong
app.jwt.expirationMs=86400000

# JWT Refresh Token
jwt.refreshExpirationMs=2592000000

# JPA Configuration
#spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.defer-datasource-initialization=true
spring.jpa.open-in-view=false

app.data-init.enabled=true

spring.profiles.active=dev

file.upload-dir=upload

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=noreplyblph@gmail.com
spring.mail.password=ikyk mqad cskr oynx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Application URL (cần thay đổi dựa trên môi trường)
app.url=http://localhost:3000
